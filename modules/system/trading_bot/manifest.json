{
    "name": "trading_bot",
    "title": "Trading Bot v1 (LIVE Executor)",
    "version": "1.0.0",
    "description": "LIVE executor: executes Brain decisions (signal.risk only).",
    "category": "system",
    "paths": {
        "storage": "storage",
        "config": "config/config.php",
        "logs": "storage/logs"
    }
}
